<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Search Results</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4OHVXqqAAgBZbQO5Q/TfylU3DRNcMtGxkgx1Bd1GtiZfUXl5RQO2YB+OstHrsF"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      /* Custom Styles */
      .card-title,
      .card-text {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }

      .card:hover {
        transform: scale(1.03);
        transition: all 0.3s ease-in-out;
      }

      .alert-warning {
        background-color: #fff4e5;
        border: 1px solid #ffc107;
        color: #856404;
      }

      .btn-outline-primary:hover {
        background-color: #007bff;
        color: #fff;
      }

      .filter-bar select {
        max-width: 300px;
      }
    </style>
  </head>
  <body>
    <div class="container py-5">
      <!-- Header Section -->
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-primary">Search Results for "<%= query %>"</h1>
        <a href="javascript:history.back()" class="btn btn-outline-secondary">Go Back</a>
      </div>

      <!-- Filter and Sort Options -->
      <div class="filter-bar d-flex justify-content-between align-items-center mb-4">
        <div>
          <select class="form-select form-select-sm" id="categoryFilter">
            <option value="">Filter by Category</option>
            <option value="Fiction">Fiction</option>
            <option value="Non-Fiction">Non-Fiction</option>
            <option value="Science">Science</option>
            <option value="History">History</option>
          </select>
        </div>
        <div>
          <select class="form-select form-select-sm" id="sortOptions">
            <option value="relevance">Sort by Relevance</option>
            <option value="title">Sort by Title</option>
            <option value="author">Sort by Author</option>
          </select>
        </div>
      </div>

      <!-- Search Results -->
      <% if (searchResults.length > 0) { %>
      <p class="text-muted">Found <strong><%= searchResults.length %></strong> result(s) matching your query.</p>
      <div class="row g-4">
        <% searchResults.forEach((book) => { %>
        <div class="col-lg-3 col-md-4 col-sm-6">
          <div class="card shadow-sm border-0 h-100">
            <!-- <img
              src="<%= book.coverImage || '/images/placeholder-book.png' %>"
              class="card-img-top"
              alt="Cover of <%= book.title %>"
              style="height: 200px; object-fit: cover; border-bottom: 1px solid #ddd"
            /> -->
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-truncate" title="<%= book.title %>"><%= book.title %></h5>
              <p class="card-text text-muted text-truncate">by <%= book.author %></p>
              <div class="mt-auto">
                <a href="/books/<%= book._id %>" class="btn btn-sm btn-outline-primary w-100">View Details</a>
              </div>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } else { %>
      <!-- No Results Found -->
      <div class="alert alert-warning text-center">
        <h4 class="alert-heading">No Results Found</h4>
        <p class="mb-0">We couldn't find any books matching your query. Try using different keywords.</p>
      </div>
      <% } %>

      <!-- Pagination (Simulated for UI Demo) -->
      <nav aria-label="Search Results Pagination" class="mt-5">
        <ul class="pagination justify-content-center">
          <li class="page-item"><a class="page-link" href="#">Previous</a></li>
          <li class="page-item active"><a class="page-link" href="#">1</a></li>
          <li class="page-item"><a class="page-link" href="#">2</a></li>
          <li class="page-item"><a class="page-link" href="#">3</a></li>
          <li class="page-item"><a class="page-link" href="#">Next</a></li>
        </ul>
      </nav>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-mQ93A+K2w9ofE5rQFurxXcxmPeIZfZ3eKqHvB8i6WgYK8R8D5E5z0O/cBeZ5cF2N"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
